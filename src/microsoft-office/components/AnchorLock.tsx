import { RawHTML } from "@src/components"

/**
 * `<w:anchorlock />`
 */
export const AnchorLock = (_props: unknown): JSX.Element => {
    return (
        <>
            <RawHTML html="<w:anchorlock />"/>
        </>
    )
}
